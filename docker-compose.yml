version: '3'

services:
  api-gpt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: Conventional_Commits_Helper
    ports:
      - 3003:3003
    restart: always
    networks:
      - home
networks:
  home:
    driver: bridge
