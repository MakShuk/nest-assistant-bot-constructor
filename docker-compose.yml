version: '3'

services:
  api-gpt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: Obsdian_Teg_Bot
    ports:
      - 4000:4000
    restart: always
    networks:
      - home
networks:
  home:
    driver: bridge
